#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: always 0 (successful)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
		putchar('\n');

	return (0);
}
