#include "main.h"
/**
 *_strlen_recursion - gets the length of a tring
 *@s: variable parameter pointer of type char
 *Return: returns the string length
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
