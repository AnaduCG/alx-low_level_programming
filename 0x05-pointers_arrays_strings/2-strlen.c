#include "main.h"
#include <string.h>
/**
 *_strlen - function that counts strings
 *@s: pointer variable of type int
 *Return: returns the string length
 */
int _strlen(char *s)
{
	return (strlen(s));
}
